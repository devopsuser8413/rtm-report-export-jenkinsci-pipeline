# ======================================================================
# 🧰 Requirements for RTM Test Execution Automation Pipeline
# ----------------------------------------------------------------------
# Covers:
#   - Selenium (for RTM Cloud export)
#   - Requests (for Confluence REST API)
#   - Email utilities (built-in smtplib used, no external dep)
#   - dotenv (optional env file handling for local testing)
#   - fpdf2 (optional report generation or conversions)
# ======================================================================

# --- Core Dependencies ---
requests==2.32.3               # REST API communication (Confluence, HTTP calls)
selenium==4.24.0               # Web automation for RTM Cloud report export
python-dotenv==1.0.1           # For optional local .env file support
fpdf2==2.8.1                   # For any PDF generation (optional future use)

# --- Utility Libraries ---
charset-normalizer==3.4.0      # Dependency of requests
certifi>=2024.7.4              # HTTPS certificates for secure connections
urllib3==2.2.3                 # HTTP transport dependency

# --- Optional Windows-specific fixes ---
# pillow==10.4.0               # Only if image embedding required in PDF
# pywin32==306                 # Only if native Win32 COM integration needed

# ======================================================================
# ✅ Tested With:
#   Python 3.10 / 3.11 / 3.12
#   Chrome 127+ / ChromeDriver 127+
#   Jenkins (Windows Agent)
# ======================================================================
